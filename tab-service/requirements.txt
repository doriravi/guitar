# IMPORTANT: requires Python 3.10. basic-pitch pins tensorflow<2.15.1, and the
# only TensorFlow builds in that range with prebuilt Windows wheels are cp310
# (TF 2.13/2.14). Python 3.11+ has no installable TF in basic-pitch's range.

# HTTP layer
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Pin numpy to 1.x — TensorFlow 2.13/2.14 and numba/librosa need numpy<2.
numpy<2

# Transcription pipeline (from fingerstyle-tab-mcp, vendored under vendor/)
basic-pitch>=0.3.0
tensorflow>=2.13.0,<2.15.1
librosa>=0.10.0
soundfile>=0.12.0
music21>=9.1.0
pyyaml>=6.0.0
demucs
